// Ponytail: these could be env vars, but they're static config that never changes in practice
export const APP_NAME = 'mtg-explorer-api';
export const APP_VERSION = '0.1.0';
export const CORS_ORIGINS = ['http://localhost:5173', 'http://localhost:3001'];
